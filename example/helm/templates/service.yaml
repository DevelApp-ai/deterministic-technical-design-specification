apiVersion: v1
kind: Service
metadata:
  name: {{ include "dtds-docs.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "dtds-docs.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "dtds-docs.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: http
